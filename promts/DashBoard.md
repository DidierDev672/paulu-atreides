Act as an expert Frontend developer and UI/UX designer. I want you to create the complete source code (Vue, CSS using TailwindCSS and Motion classes) for a homepage with a modern, responsive, and professional dashboard.

The design should incorporate the following UI/UX practices and sections:

1. Top Navigation Bar (Navbar):

Clean design with logo, search bar, notifications, user profile menu, and theme selector (light/dark).

2. Sidebar:

Clear icons and text labels for the main navigation (Dashboard, Analytics, Settings, etc.). It should be collapsible to improve the viewing area on small screens.

3. Main Section (Dashboard Grid):

Stat Cards: At least four cards at the top displaying key metrics (e.g., Revenue, Users, Conversion Rate, Tasks) with representative icons, clear numerical values, and a visual trend indicator (green for positive, red for negative).

Charts: Visual placeholders for a line chart (trend) and a pie chart (distribution). They should have a modern look, with rounded corners and sufficient spacing.

Recent Data Table: A clean table with soft corners displaying recent data (e.g., transactions or users). It should include columns with clear labels, color-coded badges, and pagination.

Recent Activity/Notifications: A side widget to display system activity or important alerts.

Technical and Design Requirements:

Apply rounded corners (rounded-xl or rounded-2xl), soft shadows (shadow-sm, shadow-md), and generous white space (p-6, gap-6) to avoid visual clutter.

Ensure it's 100% responsive, adapting perfectly from mobile screens to desktop monitors.

Use simple interactive elements with Tailwind (effects like :hover, smooth transitions).

The code should be clean, well-commented, and ready to integrate with frameworks like Vue or be used in plain HTML.

Deliver all the necessary code in a single code block so I can easily test and preview it.